{% extends 'base.html.twig' %}

{% block title %} {{ 'action.add'|trans }} {% endblock %}

{% block body %}
    
    <div class="container">
        <h1 class="mt-5">{{ 'action.add'|trans }}</h1>
        <div class="mt-5">
            {{ include('product/_form.html.twig', {'button_label': 'save'|trans }) }}
        </div>
        
    </div>

{% endblock %}
